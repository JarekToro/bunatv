// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/client/NotificationMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PlayerPath } from "../../types/playback/PlayerPath";

export const protobufPackage = "";

export interface NotificationMessage {
  notifications: string[];
  userInfos: Buffer[];
  playerPaths: PlayerPath[];
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseNotificationMessage(): NotificationMessage {
  return {
    notifications: [],
    userInfos: [],
    playerPaths: [],
    _unknownFields: {},
  };
}

export const NotificationMessage: MessageFns<NotificationMessage> = {
  encode(
    message: NotificationMessage,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    for (const v of message.notifications) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.userInfos) {
      writer.uint32(18).bytes(v!);
    }
    for (const v of message.playerPaths) {
      PlayerPath.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): NotificationMessage {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.notifications.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userInfos.push(Buffer.from(reader.bytes()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.playerPaths.push(PlayerPath.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): NotificationMessage {
    return {
      notifications: globalThis.Array.isArray(object?.notifications)
        ? object.notifications.map((e: any) => globalThis.String(e))
        : [],
      userInfos: globalThis.Array.isArray(object?.userInfos)
        ? object.userInfos.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
      playerPaths: globalThis.Array.isArray(object?.playerPaths)
        ? object.playerPaths.map((e: any) => PlayerPath.fromJSON(e))
        : [],
    };
  },

  toJSON(message: NotificationMessage): unknown {
    const obj: any = {};
    if (message.notifications?.length) {
      obj.notifications = message.notifications;
    }
    if (message.userInfos?.length) {
      obj.userInfos = message.userInfos.map((e) => base64FromBytes(e));
    }
    if (message.playerPaths?.length) {
      obj.playerPaths = message.playerPaths.map((e) => PlayerPath.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotificationMessage>, I>>(
    base?: I
  ): NotificationMessage {
    return NotificationMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotificationMessage>, I>>(
    object: I
  ): NotificationMessage {
    const message = createBaseNotificationMessage();
    message.notifications = object.notifications?.map((e) => e) || [];
    message.userInfos = object.userInfos?.map((e) => e) || [];
    message.playerPaths =
      object.playerPaths?.map((e) => PlayerPath.fromPartial(e)) || [];
    return message;
  },
};

export const notificationMessage: Extension<NotificationMessage | undefined> = {
  number: 16,
  tag: 130,
  repeated: false,
  packed: false,
  encode: (value: NotificationMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    NotificationMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): NotificationMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return NotificationMessage.decode(reader, reader.uint32());
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
