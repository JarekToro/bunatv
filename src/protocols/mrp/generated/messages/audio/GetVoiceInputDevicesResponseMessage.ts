// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/audio/GetVoiceInputDevicesResponseMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface GetVoiceInputDevicesResponseMessage {
  deviceIDs: number[];
  errorCode?: number | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseGetVoiceInputDevicesResponseMessage(): GetVoiceInputDevicesResponseMessage {
  return { deviceIDs: [], errorCode: 0, _unknownFields: {} };
}

export const GetVoiceInputDevicesResponseMessage: MessageFns<GetVoiceInputDevicesResponseMessage> =
  {
    encode(
      message: GetVoiceInputDevicesResponseMessage,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      for (const v of message.deviceIDs) {
        writer.uint32(8).uint32(v!);
      }
      if (message.errorCode !== undefined && message.errorCode !== 0) {
        writer.uint32(16).uint32(message.errorCode);
      }
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): GetVoiceInputDevicesResponseMessage {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetVoiceInputDevicesResponseMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag === 8) {
              message.deviceIDs.push(reader.uint32());

              continue;
            }

            if (tag === 10) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.deviceIDs.push(reader.uint32());
              }

              continue;
            }

            break;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.errorCode = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(object: any): GetVoiceInputDevicesResponseMessage {
      return {
        deviceIDs: globalThis.Array.isArray(object?.deviceIDs)
          ? object.deviceIDs.map((e: any) => globalThis.Number(e))
          : [],
        errorCode: isSet(object.errorCode)
          ? globalThis.Number(object.errorCode)
          : 0,
      };
    },

    toJSON(message: GetVoiceInputDevicesResponseMessage): unknown {
      const obj: any = {};
      if (message.deviceIDs?.length) {
        obj.deviceIDs = message.deviceIDs.map((e) => Math.round(e));
      }
      if (message.errorCode !== undefined && message.errorCode !== 0) {
        obj.errorCode = Math.round(message.errorCode);
      }
      return obj;
    },

    create<
      I extends Exact<DeepPartial<GetVoiceInputDevicesResponseMessage>, I>,
    >(base?: I): GetVoiceInputDevicesResponseMessage {
      return GetVoiceInputDevicesResponseMessage.fromPartial(
        base ?? ({} as any)
      );
    },
    fromPartial<
      I extends Exact<DeepPartial<GetVoiceInputDevicesResponseMessage>, I>,
    >(object: I): GetVoiceInputDevicesResponseMessage {
      const message = createBaseGetVoiceInputDevicesResponseMessage();
      message.deviceIDs = object.deviceIDs?.map((e) => e) || [];
      message.errorCode = object.errorCode ?? 0;
      return message;
    },
  };

export const getVoiceInputDevicesResponseMessage: Extension<
  GetVoiceInputDevicesResponseMessage | undefined
> = {
  number: 32,
  tag: 258,
  repeated: false,
  packed: false,
  encode: (
    value: GetVoiceInputDevicesResponseMessage | undefined
  ): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    GetVoiceInputDevicesResponseMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): GetVoiceInputDevicesResponseMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return GetVoiceInputDevicesResponseMessage.decode(reader, reader.uint32());
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
