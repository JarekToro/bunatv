// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/audio/GetVolumeMutedMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

/** Request to get muted state of an output device */
export interface GetVolumeMutedMessage {
  outputDeviceUID?: string | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseGetVolumeMutedMessage(): GetVolumeMutedMessage {
  return { outputDeviceUID: "", _unknownFields: {} };
}

export const GetVolumeMutedMessage: MessageFns<GetVolumeMutedMessage> = {
  encode(
    message: GetVolumeMutedMessage,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (
      message.outputDeviceUID !== undefined &&
      message.outputDeviceUID !== ""
    ) {
      writer.uint32(10).string(message.outputDeviceUID);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): GetVolumeMutedMessage {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVolumeMutedMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.outputDeviceUID = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): GetVolumeMutedMessage {
    return {
      outputDeviceUID: isSet(object.outputDeviceUID)
        ? globalThis.String(object.outputDeviceUID)
        : "",
    };
  },

  toJSON(message: GetVolumeMutedMessage): unknown {
    const obj: any = {};
    if (
      message.outputDeviceUID !== undefined &&
      message.outputDeviceUID !== ""
    ) {
      obj.outputDeviceUID = message.outputDeviceUID;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetVolumeMutedMessage>, I>>(
    base?: I
  ): GetVolumeMutedMessage {
    return GetVolumeMutedMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetVolumeMutedMessage>, I>>(
    object: I
  ): GetVolumeMutedMessage {
    const message = createBaseGetVolumeMutedMessage();
    message.outputDeviceUID = object.outputDeviceUID ?? "";
    return message;
  },
};

export const getVolumeMutedMessage: Extension<
  GetVolumeMutedMessage | undefined
> = {
  number: 98,
  tag: 786,
  repeated: false,
  packed: false,
  encode: (value: GetVolumeMutedMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    GetVolumeMutedMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): GetVolumeMutedMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return GetVolumeMutedMessage.decode(reader, reader.uint32());
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
