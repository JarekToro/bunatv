// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/input/SendVirtualTouchEventMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { VirtualTouchEvent } from "../../types/input/VirtualTouchEvent";

export const protobufPackage = "";

export interface SendVirtualTouchEventMessage {
  virtualDeviceID?: number | undefined;
  event?: VirtualTouchEvent | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseSendVirtualTouchEventMessage(): SendVirtualTouchEventMessage {
  return { virtualDeviceID: 0, event: undefined, _unknownFields: {} };
}

export const SendVirtualTouchEventMessage: MessageFns<SendVirtualTouchEventMessage> =
  {
    encode(
      message: SendVirtualTouchEventMessage,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      if (
        message.virtualDeviceID !== undefined &&
        message.virtualDeviceID !== 0
      ) {
        writer.uint32(8).uint64(message.virtualDeviceID);
      }
      if (message.event !== undefined) {
        VirtualTouchEvent.encode(
          message.event,
          writer.uint32(18).fork()
        ).join();
      }
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): SendVirtualTouchEventMessage {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSendVirtualTouchEventMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.virtualDeviceID = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.event = VirtualTouchEvent.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(object: any): SendVirtualTouchEventMessage {
      return {
        virtualDeviceID: isSet(object.virtualDeviceID)
          ? globalThis.Number(object.virtualDeviceID)
          : 0,
        event: isSet(object.event)
          ? VirtualTouchEvent.fromJSON(object.event)
          : undefined,
      };
    },

    toJSON(message: SendVirtualTouchEventMessage): unknown {
      const obj: any = {};
      if (
        message.virtualDeviceID !== undefined &&
        message.virtualDeviceID !== 0
      ) {
        obj.virtualDeviceID = Math.round(message.virtualDeviceID);
      }
      if (message.event !== undefined) {
        obj.event = VirtualTouchEvent.toJSON(message.event);
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<SendVirtualTouchEventMessage>, I>>(
      base?: I
    ): SendVirtualTouchEventMessage {
      return SendVirtualTouchEventMessage.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<SendVirtualTouchEventMessage>, I>>(
      object: I
    ): SendVirtualTouchEventMessage {
      const message = createBaseSendVirtualTouchEventMessage();
      message.virtualDeviceID = object.virtualDeviceID ?? 0;
      message.event =
        object.event !== undefined && object.event !== null
          ? VirtualTouchEvent.fromPartial(object.event)
          : undefined;
      return message;
    },
  };

export const sendVirtualTouchEventMessage: Extension<
  SendVirtualTouchEventMessage | undefined
> = {
  number: 15,
  tag: 122,
  repeated: false,
  packed: false,
  encode: (value: SendVirtualTouchEventMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    SendVirtualTouchEventMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): SendVirtualTouchEventMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return SendVirtualTouchEventMessage.decode(reader, reader.uint32());
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
