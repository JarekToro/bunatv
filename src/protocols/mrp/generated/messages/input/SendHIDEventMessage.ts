// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/input/SendHIDEventMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface SendHIDEventMessage {
  /**
   * This data corresponds to a "keyboardEvent" in IOHIDEvent.h encoded as raw
   * data. Here is one source:
   *
   *   https://opensource.apple.com/source/IOHIDFamily/IOHIDFamily-308/IOHIDFamily/IOHIDEvent.h.auto.html
   *
   * The interesting parts are:
   *  - usagePage (UInt32)
   *  - usage (Uint32)
   *  - down (bool)
   *
   * The parameters usagePage and usage corresponds to the key being pressed.
   * It is mapped to the USB HID values, which can be found here:
   *
   *  https://github.com/Daij-Djan/DDHidLib/blob/master/usb_hid_usages.txt
   *
   * Pressing left key would for instance map to usagePage=0x01, usage=0x8B. In
   * the hid data, these values are stored as big endian uint16 values in the
   * mentioned order. So the same example would be: 0x0001008B0001, assuming
   * down = true (key being pressed). For each key press, the same usagePage
   * and usage are sent with down=true and down=false (key down + key up).
   *
   * There is a bit of magic in the raw data that's just not decoded yet, but
   * that doesn't matter. Just use this and it will work:
   *
   *  438922cf080200000000000000000000010000000000000002000000200000000300000001000000000000<data>0000000000000001000000
   *
   * <data> corresponds to the values above, e.g. 0001008B0001. The first 8
   * bytes is a timestamp (mach AbsoluteTime). It's a bit tricky to derive but
   * tvOS seems to accept old timestamps here. So it's probably fine to send
   * anything.
   */
  hidEventData?: Buffer | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseSendHIDEventMessage(): SendHIDEventMessage {
  return { hidEventData: undefined, _unknownFields: {} };
}

export const SendHIDEventMessage: MessageFns<SendHIDEventMessage> = {
  encode(
    message: SendHIDEventMessage,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.hidEventData !== undefined) {
      writer.uint32(10).bytes(message.hidEventData);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): SendHIDEventMessage {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendHIDEventMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hidEventData = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): SendHIDEventMessage {
    return {
      hidEventData: isSet(object.hidEventData)
        ? Buffer.from(bytesFromBase64(object.hidEventData))
        : undefined,
    };
  },

  toJSON(message: SendHIDEventMessage): unknown {
    const obj: any = {};
    if (message.hidEventData !== undefined) {
      obj.hidEventData = base64FromBytes(message.hidEventData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SendHIDEventMessage>, I>>(
    base?: I
  ): SendHIDEventMessage {
    return SendHIDEventMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendHIDEventMessage>, I>>(
    object: I
  ): SendHIDEventMessage {
    const message = createBaseSendHIDEventMessage();
    message.hidEventData = object.hidEventData ?? undefined;
    return message;
  },
};

export const sendHIDEventMessage: Extension<SendHIDEventMessage | undefined> = {
  number: 13,
  tag: 106,
  repeated: false,
  packed: false,
  encode: (value: SendHIDEventMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    SendHIDEventMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): SendHIDEventMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return SendHIDEventMessage.decode(reader, reader.uint32());
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
