// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: messages/playback/PlaybackSessionMigrateEndMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Error } from "../../foundation/Error";
import { PlaybackSessionRequest } from "../../types/playback/PlaybackSessionRequest";
import { PlayerPath } from "../../types/playback/PlayerPath";
import { SendCommandResultStatus } from "./SendCommandResultMessage";

export const protobufPackage = "";

export interface PlaybackSessionMigrateEndMessage {
  request?: PlaybackSessionRequest | undefined;
  playerPath?: PlayerPath | undefined;
  errorCode?: number | undefined;
  errorDescription?: string | undefined;
  error?: Error | undefined;
  setPlaybackSessionCommandStatus?: SendCommandResultStatus | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBasePlaybackSessionMigrateEndMessage(): PlaybackSessionMigrateEndMessage {
  return {
    request: undefined,
    playerPath: undefined,
    errorCode: 0,
    errorDescription: "",
    error: undefined,
    setPlaybackSessionCommandStatus: undefined,
    _unknownFields: {},
  };
}

export const PlaybackSessionMigrateEndMessage: MessageFns<PlaybackSessionMigrateEndMessage> =
  {
    encode(
      message: PlaybackSessionMigrateEndMessage,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      if (message.request !== undefined) {
        PlaybackSessionRequest.encode(
          message.request,
          writer.uint32(10).fork()
        ).join();
      }
      if (message.playerPath !== undefined) {
        PlayerPath.encode(message.playerPath, writer.uint32(18).fork()).join();
      }
      if (message.errorCode !== undefined && message.errorCode !== 0) {
        writer.uint32(24).int64(message.errorCode);
      }
      if (
        message.errorDescription !== undefined &&
        message.errorDescription !== ""
      ) {
        writer.uint32(34).string(message.errorDescription);
      }
      if (message.error !== undefined) {
        Error.encode(message.error, writer.uint32(42).fork()).join();
      }
      if (message.setPlaybackSessionCommandStatus !== undefined) {
        SendCommandResultStatus.encode(
          message.setPlaybackSessionCommandStatus,
          writer.uint32(50).fork()
        ).join();
      }
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): PlaybackSessionMigrateEndMessage {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePlaybackSessionMigrateEndMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.request = PlaybackSessionRequest.decode(
              reader,
              reader.uint32()
            );
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.playerPath = PlayerPath.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.errorCode = longToNumber(reader.int64());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.errorDescription = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.error = Error.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.setPlaybackSessionCommandStatus =
              SendCommandResultStatus.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(object: any): PlaybackSessionMigrateEndMessage {
      return {
        request: isSet(object.request)
          ? PlaybackSessionRequest.fromJSON(object.request)
          : undefined,
        playerPath: isSet(object.playerPath)
          ? PlayerPath.fromJSON(object.playerPath)
          : undefined,
        errorCode: isSet(object.errorCode)
          ? globalThis.Number(object.errorCode)
          : 0,
        errorDescription: isSet(object.errorDescription)
          ? globalThis.String(object.errorDescription)
          : "",
        error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
        setPlaybackSessionCommandStatus: isSet(
          object.setPlaybackSessionCommandStatus
        )
          ? SendCommandResultStatus.fromJSON(
              object.setPlaybackSessionCommandStatus
            )
          : undefined,
      };
    },

    toJSON(message: PlaybackSessionMigrateEndMessage): unknown {
      const obj: any = {};
      if (message.request !== undefined) {
        obj.request = PlaybackSessionRequest.toJSON(message.request);
      }
      if (message.playerPath !== undefined) {
        obj.playerPath = PlayerPath.toJSON(message.playerPath);
      }
      if (message.errorCode !== undefined && message.errorCode !== 0) {
        obj.errorCode = Math.round(message.errorCode);
      }
      if (
        message.errorDescription !== undefined &&
        message.errorDescription !== ""
      ) {
        obj.errorDescription = message.errorDescription;
      }
      if (message.error !== undefined) {
        obj.error = Error.toJSON(message.error);
      }
      if (message.setPlaybackSessionCommandStatus !== undefined) {
        obj.setPlaybackSessionCommandStatus = SendCommandResultStatus.toJSON(
          message.setPlaybackSessionCommandStatus
        );
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<PlaybackSessionMigrateEndMessage>, I>>(
      base?: I
    ): PlaybackSessionMigrateEndMessage {
      return PlaybackSessionMigrateEndMessage.fromPartial(base ?? ({} as any));
    },
    fromPartial<
      I extends Exact<DeepPartial<PlaybackSessionMigrateEndMessage>, I>,
    >(object: I): PlaybackSessionMigrateEndMessage {
      const message = createBasePlaybackSessionMigrateEndMessage();
      message.request =
        object.request !== undefined && object.request !== null
          ? PlaybackSessionRequest.fromPartial(object.request)
          : undefined;
      message.playerPath =
        object.playerPath !== undefined && object.playerPath !== null
          ? PlayerPath.fromPartial(object.playerPath)
          : undefined;
      message.errorCode = object.errorCode ?? 0;
      message.errorDescription = object.errorDescription ?? "";
      message.error =
        object.error !== undefined && object.error !== null
          ? Error.fromPartial(object.error)
          : undefined;
      message.setPlaybackSessionCommandStatus =
        object.setPlaybackSessionCommandStatus !== undefined &&
        object.setPlaybackSessionCommandStatus !== null
          ? SendCommandResultStatus.fromPartial(
              object.setPlaybackSessionCommandStatus
            )
          : undefined;
      return message;
    },
  };

export const playbackSessionMigrateEndMessage: Extension<
  PlaybackSessionMigrateEndMessage | undefined
> = {
  number: 80,
  tag: 642,
  repeated: false,
  packed: false,
  encode: (
    value: PlaybackSessionMigrateEndMessage | undefined
  ): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    if (value === undefined) {
      return encoded;
    }
    const writer = new BinaryWriter();
    PlaybackSessionMigrateEndMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): PlaybackSessionMigrateEndMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return PlaybackSessionMigrateEndMessage.decode(reader, reader.uint32());
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
