// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: RemoteTextInputMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = ''

export interface RemoteTextInputMessage {
  timestamp?: number | undefined
  version?: number | undefined
  data?: Buffer | undefined
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined
}

function createBaseRemoteTextInputMessage(): RemoteTextInputMessage {
  return { timestamp: 0, version: 0, data: Buffer.alloc(0), _unknownFields: {} }
}

export const RemoteTextInputMessage: MessageFns<RemoteTextInputMessage> = {
  encode(message: RemoteTextInputMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined && message.timestamp !== 0) {
      writer.uint32(9).double(message.timestamp)
    }
    if (message.version !== undefined && message.version !== 0) {
      writer.uint32(16).uint64(message.version)
    }
    if (message.data !== undefined && message.data.length !== 0) {
      writer.uint32(26).bytes(message.data)
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10)
        for (const value of values) {
          writer.uint32(tag).raw(value)
        }
      }
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoteTextInputMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRemoteTextInputMessage()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break
          }

          message.timestamp = reader.double()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.version = longToNumber(reader.uint64())
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.data = Buffer.from(reader.bytes())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      const buf = reader.skip(tag & 7)

      const list = message._unknownFields![tag]

      if (list === undefined) {
        message._unknownFields![tag] = [buf]
      } else {
        list.push(buf)
      }
    }
    return message
  },

  fromJSON(object: any): RemoteTextInputMessage {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0),
    }
  },

  toJSON(message: RemoteTextInputMessage): unknown {
    const obj: any = {}
    if (message.timestamp !== undefined && message.timestamp !== 0) {
      obj.timestamp = message.timestamp
    }
    if (message.version !== undefined && message.version !== 0) {
      obj.version = Math.round(message.version)
    }
    if (message.data !== undefined && message.data.length !== 0) {
      obj.data = base64FromBytes(message.data)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<RemoteTextInputMessage>, I>>(
    base?: I
  ): RemoteTextInputMessage {
    return RemoteTextInputMessage.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<RemoteTextInputMessage>, I>>(
    object: I
  ): RemoteTextInputMessage {
    const message = createBaseRemoteTextInputMessage()
    message.timestamp = object.timestamp ?? 0
    message.version = object.version ?? 0
    message.data = object.data ?? Buffer.alloc(0)
    return message
  },
}

export const remoteTextInputMessage: Extension<RemoteTextInputMessage | undefined> = {
  number: 71,
  tag: 570,
  repeated: false,
  packed: false,
  encode: (value: RemoteTextInputMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = []
    const writer = new BinaryWriter()
    RemoteTextInputMessage.encode(value, writer.fork()).join()
    encoded.push(writer.finish())
    return encoded
  },
  decode: (tag: number, input: Uint8Array[]): RemoteTextInputMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail())
    return RemoteTextInputMessage.decode(reader, reader.uint32())
  },
}

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, 'base64'))
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString('base64')
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface Extension<T> {
  number: number
  tag: number
  singularTag?: number
  packedTag?: number
  encode?: (message: T) => Uint8Array[]
  decode?: (tag: number, input: Uint8Array[]) => T
  repeated: boolean
  packed: boolean
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? 'Failed')
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
