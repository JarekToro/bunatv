// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: GetVolumeResultMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = ''

export interface GetVolumeResultMessage {
  volume?: number | undefined
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined
}

function createBaseGetVolumeResultMessage(): GetVolumeResultMessage {
  return { volume: 0, _unknownFields: {} }
}

export const GetVolumeResultMessage: MessageFns<GetVolumeResultMessage> = {
  encode(message: GetVolumeResultMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.volume !== undefined && message.volume !== 0) {
      writer.uint32(13).float(message.volume)
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10)
        for (const value of values) {
          writer.uint32(tag).raw(value)
        }
      }
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVolumeResultMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseGetVolumeResultMessage()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break
          }

          message.volume = reader.float()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      const buf = reader.skip(tag & 7)

      const list = message._unknownFields![tag]

      if (list === undefined) {
        message._unknownFields![tag] = [buf]
      } else {
        list.push(buf)
      }
    }
    return message
  },

  fromJSON(object: any): GetVolumeResultMessage {
    return { volume: isSet(object.volume) ? globalThis.Number(object.volume) : 0 }
  },

  toJSON(message: GetVolumeResultMessage): unknown {
    const obj: any = {}
    if (message.volume !== undefined && message.volume !== 0) {
      obj.volume = message.volume
    }
    return obj
  },

  create<I extends Exact<DeepPartial<GetVolumeResultMessage>, I>>(
    base?: I
  ): GetVolumeResultMessage {
    return GetVolumeResultMessage.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<GetVolumeResultMessage>, I>>(
    object: I
  ): GetVolumeResultMessage {
    const message = createBaseGetVolumeResultMessage()
    message.volume = object.volume ?? 0
    return message
  },
}

export const getVolumeResultMessage: Extension<GetVolumeResultMessage | undefined> = {
  number: 54,
  tag: 434,
  repeated: false,
  packed: false,
  encode: (value: GetVolumeResultMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = []
    const writer = new BinaryWriter()
    GetVolumeResultMessage.encode(value, writer.fork()).join()
    encoded.push(writer.finish())
    return encoded
  },
  decode: (tag: number, input: Uint8Array[]): GetVolumeResultMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail())
    return GetVolumeResultMessage.decode(reader, reader.uint32())
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface Extension<T> {
  number: number
  tag: number
  singularTag?: number
  packedTag?: number
  encode?: (message: T) => Uint8Array[]
  decode?: (tag: number, input: Uint8Array[]) => T
  repeated: boolean
  packed: boolean
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? 'Failed')
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
