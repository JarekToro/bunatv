// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: types/playback/NowPlayingPlayer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface NowPlayingPlayer {
  identifier?: string | undefined;
  displayName?: string | undefined;
  /** NOTE(RE): field 3 (isDefaultPlayer) is absent in decompiled code */
  isDefaultPlayer?: boolean | undefined;
  audioSessionType?: NowPlayingPlayer_AudioSessionType | undefined;
  mxSessionIDs: number[];
  audioSessionID?: number | undefined;
  iconURL?: string | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export enum NowPlayingPlayer_AudioSessionType {
  Default = 0,
  LongForm = 1,
  Independent = 2,
  UNRECOGNIZED = -1,
}

export function nowPlayingPlayer_AudioSessionTypeFromJSON(
  object: any
): NowPlayingPlayer_AudioSessionType {
  switch (object) {
    case 0:
    case "Default":
      return NowPlayingPlayer_AudioSessionType.Default;
    case 1:
    case "LongForm":
      return NowPlayingPlayer_AudioSessionType.LongForm;
    case 2:
    case "Independent":
      return NowPlayingPlayer_AudioSessionType.Independent;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NowPlayingPlayer_AudioSessionType.UNRECOGNIZED;
  }
}

export function nowPlayingPlayer_AudioSessionTypeToJSON(
  object: NowPlayingPlayer_AudioSessionType
): string {
  switch (object) {
    case NowPlayingPlayer_AudioSessionType.Default:
      return "Default";
    case NowPlayingPlayer_AudioSessionType.LongForm:
      return "LongForm";
    case NowPlayingPlayer_AudioSessionType.Independent:
      return "Independent";
    case NowPlayingPlayer_AudioSessionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseNowPlayingPlayer(): NowPlayingPlayer {
  return {
    identifier: undefined,
    displayName: undefined,
    isDefaultPlayer: undefined,
    audioSessionType: undefined,
    mxSessionIDs: [],
    audioSessionID: undefined,
    iconURL: undefined,
    _unknownFields: {},
  };
}

export const NowPlayingPlayer: MessageFns<NowPlayingPlayer> = {
  encode(
    message: NowPlayingPlayer,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.identifier !== undefined) {
      writer.uint32(10).string(message.identifier);
    }
    if (message.displayName !== undefined) {
      writer.uint32(18).string(message.displayName);
    }
    if (message.isDefaultPlayer !== undefined) {
      writer.uint32(24).bool(message.isDefaultPlayer);
    }
    if (message.audioSessionType !== undefined) {
      writer.uint32(32).int32(message.audioSessionType);
    }
    for (const v of message.mxSessionIDs) {
      writer.uint32(40).int64(v!);
    }
    if (message.audioSessionID !== undefined) {
      writer.uint32(48).uint32(message.audioSessionID);
    }
    if (message.iconURL !== undefined) {
      writer.uint32(58).string(message.iconURL);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NowPlayingPlayer {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNowPlayingPlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isDefaultPlayer = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.audioSessionType = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.mxSessionIDs.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.mxSessionIDs.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.audioSessionID = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.iconURL = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): NowPlayingPlayer {
    return {
      identifier: isSet(object.identifier)
        ? globalThis.String(object.identifier)
        : undefined,
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : undefined,
      isDefaultPlayer: isSet(object.isDefaultPlayer)
        ? globalThis.Boolean(object.isDefaultPlayer)
        : undefined,
      audioSessionType: isSet(object.audioSessionType)
        ? nowPlayingPlayer_AudioSessionTypeFromJSON(object.audioSessionType)
        : undefined,
      mxSessionIDs: globalThis.Array.isArray(object?.mxSessionIDs)
        ? object.mxSessionIDs.map((e: any) => globalThis.Number(e))
        : [],
      audioSessionID: isSet(object.audioSessionID)
        ? globalThis.Number(object.audioSessionID)
        : undefined,
      iconURL: isSet(object.iconURL)
        ? globalThis.String(object.iconURL)
        : undefined,
    };
  },

  toJSON(message: NowPlayingPlayer): unknown {
    const obj: any = {};
    if (message.identifier !== undefined) {
      obj.identifier = message.identifier;
    }
    if (message.displayName !== undefined) {
      obj.displayName = message.displayName;
    }
    if (message.isDefaultPlayer !== undefined) {
      obj.isDefaultPlayer = message.isDefaultPlayer;
    }
    if (message.audioSessionType !== undefined) {
      obj.audioSessionType = nowPlayingPlayer_AudioSessionTypeToJSON(
        message.audioSessionType
      );
    }
    if (message.mxSessionIDs?.length) {
      obj.mxSessionIDs = message.mxSessionIDs.map((e) => Math.round(e));
    }
    if (message.audioSessionID !== undefined) {
      obj.audioSessionID = Math.round(message.audioSessionID);
    }
    if (message.iconURL !== undefined) {
      obj.iconURL = message.iconURL;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NowPlayingPlayer>, I>>(
    base?: I
  ): NowPlayingPlayer {
    return NowPlayingPlayer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NowPlayingPlayer>, I>>(
    object: I
  ): NowPlayingPlayer {
    const message = createBaseNowPlayingPlayer();
    message.identifier = object.identifier ?? undefined;
    message.displayName = object.displayName ?? undefined;
    message.isDefaultPlayer = object.isDefaultPlayer ?? undefined;
    message.audioSessionType = object.audioSessionType ?? undefined;
    message.mxSessionIDs = object.mxSessionIDs?.map((e) => e) || [];
    message.audioSessionID = object.audioSessionID ?? undefined;
    message.iconURL = object.iconURL ?? undefined;
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
