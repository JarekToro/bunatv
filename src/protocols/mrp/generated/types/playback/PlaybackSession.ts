// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: types/playback/PlaybackSession.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface PlaybackSession {
  playbackSessionData?: Buffer | undefined;
  identifier?: string | undefined;
  type?: string | undefined;
  revision?: string | undefined;
  metadata?: Buffer | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBasePlaybackSession(): PlaybackSession {
  return {
    playbackSessionData: Buffer.alloc(0),
    identifier: "",
    type: "",
    revision: "",
    metadata: Buffer.alloc(0),
    _unknownFields: {},
  };
}

export const PlaybackSession: MessageFns<PlaybackSession> = {
  encode(
    message: PlaybackSession,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (
      message.playbackSessionData !== undefined &&
      message.playbackSessionData.length !== 0
    ) {
      writer.uint32(10).bytes(message.playbackSessionData);
    }
    if (message.identifier !== undefined && message.identifier !== "") {
      writer.uint32(18).string(message.identifier);
    }
    if (message.type !== undefined && message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.revision !== undefined && message.revision !== "") {
      writer.uint32(34).string(message.revision);
    }
    if (message.metadata !== undefined && message.metadata.length !== 0) {
      writer.uint32(42).bytes(message.metadata);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaybackSession {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaybackSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playbackSessionData = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.identifier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.revision = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.metadata = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): PlaybackSession {
    return {
      playbackSessionData: isSet(object.playbackSessionData)
        ? Buffer.from(bytesFromBase64(object.playbackSessionData))
        : Buffer.alloc(0),
      identifier: isSet(object.identifier)
        ? globalThis.String(object.identifier)
        : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      revision: isSet(object.revision)
        ? globalThis.String(object.revision)
        : "",
      metadata: isSet(object.metadata)
        ? Buffer.from(bytesFromBase64(object.metadata))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: PlaybackSession): unknown {
    const obj: any = {};
    if (
      message.playbackSessionData !== undefined &&
      message.playbackSessionData.length !== 0
    ) {
      obj.playbackSessionData = base64FromBytes(message.playbackSessionData);
    }
    if (message.identifier !== undefined && message.identifier !== "") {
      obj.identifier = message.identifier;
    }
    if (message.type !== undefined && message.type !== "") {
      obj.type = message.type;
    }
    if (message.revision !== undefined && message.revision !== "") {
      obj.revision = message.revision;
    }
    if (message.metadata !== undefined && message.metadata.length !== 0) {
      obj.metadata = base64FromBytes(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlaybackSession>, I>>(
    base?: I
  ): PlaybackSession {
    return PlaybackSession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlaybackSession>, I>>(
    object: I
  ): PlaybackSession {
    const message = createBasePlaybackSession();
    message.playbackSessionData = object.playbackSessionData ?? Buffer.alloc(0);
    message.identifier = object.identifier ?? "";
    message.type = object.type ?? "";
    message.revision = object.revision ?? "";
    message.metadata = object.metadata ?? Buffer.alloc(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
