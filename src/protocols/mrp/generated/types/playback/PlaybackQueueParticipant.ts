// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: types/playback/PlaybackQueueParticipant.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UserIdentity } from "../session/UserIdentity";

export const protobufPackage = "";

export interface PlaybackQueueParticipant {
  identifier?: string | undefined;
  identity?: UserIdentity | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBasePlaybackQueueParticipant(): PlaybackQueueParticipant {
  return { identifier: "", identity: undefined, _unknownFields: {} };
}

export const PlaybackQueueParticipant: MessageFns<PlaybackQueueParticipant> = {
  encode(
    message: PlaybackQueueParticipant,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.identifier !== undefined && message.identifier !== "") {
      writer.uint32(10).string(message.identifier);
    }
    if (message.identity !== undefined) {
      UserIdentity.encode(message.identity, writer.uint32(18).fork()).join();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): PlaybackQueueParticipant {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaybackQueueParticipant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.identity = UserIdentity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): PlaybackQueueParticipant {
    return {
      identifier: isSet(object.identifier)
        ? globalThis.String(object.identifier)
        : "",
      identity: isSet(object.identity)
        ? UserIdentity.fromJSON(object.identity)
        : undefined,
    };
  },

  toJSON(message: PlaybackQueueParticipant): unknown {
    const obj: any = {};
    if (message.identifier !== undefined && message.identifier !== "") {
      obj.identifier = message.identifier;
    }
    if (message.identity !== undefined) {
      obj.identity = UserIdentity.toJSON(message.identity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlaybackQueueParticipant>, I>>(
    base?: I
  ): PlaybackQueueParticipant {
    return PlaybackQueueParticipant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlaybackQueueParticipant>, I>>(
    object: I
  ): PlaybackQueueParticipant {
    const message = createBasePlaybackQueueParticipant();
    message.identifier = object.identifier ?? "";
    message.identity =
      object.identity !== undefined && object.identity !== null
        ? UserIdentity.fromPartial(object.identity)
        : undefined;
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
