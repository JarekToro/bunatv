// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: types/media/AnimatedArtwork.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DataArtwork } from "./DataArtwork";

export const protobufPackage = "";

export interface AnimatedArtwork {
  frames: DataArtwork[];
  frameDuration?: number | undefined;
  loop?: boolean | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseAnimatedArtwork(): AnimatedArtwork {
  return { frames: [], frameDuration: 0, loop: false, _unknownFields: {} };
}

export const AnimatedArtwork: MessageFns<AnimatedArtwork> = {
  encode(
    message: AnimatedArtwork,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    for (const v of message.frames) {
      DataArtwork.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.frameDuration !== undefined && message.frameDuration !== 0) {
      writer.uint32(17).double(message.frameDuration);
    }
    if (message.loop !== undefined && message.loop !== false) {
      writer.uint32(24).bool(message.loop);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnimatedArtwork {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnimatedArtwork();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.frames.push(DataArtwork.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.frameDuration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.loop = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): AnimatedArtwork {
    return {
      frames: globalThis.Array.isArray(object?.frames)
        ? object.frames.map((e: any) => DataArtwork.fromJSON(e))
        : [],
      frameDuration: isSet(object.frameDuration)
        ? globalThis.Number(object.frameDuration)
        : isSet(object.frame_duration)
          ? globalThis.Number(object.frame_duration)
          : 0,
      loop: isSet(object.loop) ? globalThis.Boolean(object.loop) : false,
    };
  },

  toJSON(message: AnimatedArtwork): unknown {
    const obj: any = {};
    if (message.frames?.length) {
      obj.frames = message.frames.map((e) => DataArtwork.toJSON(e));
    }
    if (message.frameDuration !== undefined && message.frameDuration !== 0) {
      obj.frameDuration = message.frameDuration;
    }
    if (message.loop !== undefined && message.loop !== false) {
      obj.loop = message.loop;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnimatedArtwork>, I>>(
    base?: I
  ): AnimatedArtwork {
    return AnimatedArtwork.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnimatedArtwork>, I>>(
    object: I
  ): AnimatedArtwork {
    const message = createBaseAnimatedArtwork();
    message.frames =
      object.frames?.map((e) => DataArtwork.fromPartial(e)) || [];
    message.frameDuration = object.frameDuration ?? 0;
    message.loop = object.loop ?? false;
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
