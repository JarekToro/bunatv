// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: types/media/SystemPlaybackQueue.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  PlaybackQueueType_Enum,
  playbackQueueType_EnumFromJSON,
  playbackQueueType_EnumToJSON,
  ReplaceIntent_Enum,
  replaceIntent_EnumFromJSON,
  replaceIntent_EnumToJSON,
} from "../../foundation/Common";
import { SystemPlaybackCustomDataQueue } from "./SystemPlaybackCustomDataQueue";
import { SystemPlaybackGenericTracklistQueue } from "./SystemPlaybackGenericTrackListQueue";

export const protobufPackage = "";

export interface SystemPlaybackQueue {
  type?: PlaybackQueueType_Enum | undefined;
  replaceIntent?: ReplaceIntent_Enum | undefined;
  isRequestingImmediatePlayback?: boolean | undefined;
  featureName?: string | undefined;
  customData?: SystemPlaybackCustomDataQueue | undefined;
  tracklist?: SystemPlaybackGenericTracklistQueue | undefined;
  userInfo?: Buffer | undefined;
  metrics?: Buffer | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseSystemPlaybackQueue(): SystemPlaybackQueue {
  return {
    type: 1,
    replaceIntent: 1,
    isRequestingImmediatePlayback: false,
    featureName: "",
    customData: undefined,
    tracklist: undefined,
    userInfo: Buffer.alloc(0),
    metrics: Buffer.alloc(0),
    _unknownFields: {},
  };
}

export const SystemPlaybackQueue: MessageFns<SystemPlaybackQueue> = {
  encode(
    message: SystemPlaybackQueue,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.type !== undefined && message.type !== 1) {
      writer.uint32(8).int32(message.type);
    }
    if (message.replaceIntent !== undefined && message.replaceIntent !== 1) {
      writer.uint32(16).int32(message.replaceIntent);
    }
    if (
      message.isRequestingImmediatePlayback !== undefined &&
      message.isRequestingImmediatePlayback !== false
    ) {
      writer.uint32(24).bool(message.isRequestingImmediatePlayback);
    }
    if (message.featureName !== undefined && message.featureName !== "") {
      writer.uint32(34).string(message.featureName);
    }
    if (message.customData !== undefined) {
      SystemPlaybackCustomDataQueue.encode(
        message.customData,
        writer.uint32(50).fork()
      ).join();
    }
    if (message.tracklist !== undefined) {
      SystemPlaybackGenericTracklistQueue.encode(
        message.tracklist,
        writer.uint32(58).fork()
      ).join();
    }
    if (message.userInfo !== undefined && message.userInfo.length !== 0) {
      writer.uint32(66).bytes(message.userInfo);
    }
    if (message.metrics !== undefined && message.metrics.length !== 0) {
      writer.uint32(74).bytes(message.metrics);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): SystemPlaybackQueue {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemPlaybackQueue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.replaceIntent = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isRequestingImmediatePlayback = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.featureName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.customData = SystemPlaybackCustomDataQueue.decode(
            reader,
            reader.uint32()
          );
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tracklist = SystemPlaybackGenericTracklistQueue.decode(
            reader,
            reader.uint32()
          );
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.userInfo = Buffer.from(reader.bytes());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.metrics = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): SystemPlaybackQueue {
    return {
      type: isSet(object.type)
        ? playbackQueueType_EnumFromJSON(object.type)
        : 1,
      replaceIntent: isSet(object.replaceIntent)
        ? replaceIntent_EnumFromJSON(object.replaceIntent)
        : 1,
      isRequestingImmediatePlayback: isSet(object.isRequestingImmediatePlayback)
        ? globalThis.Boolean(object.isRequestingImmediatePlayback)
        : false,
      featureName: isSet(object.featureName)
        ? globalThis.String(object.featureName)
        : "",
      customData: isSet(object.customData)
        ? SystemPlaybackCustomDataQueue.fromJSON(object.customData)
        : undefined,
      tracklist: isSet(object.tracklist)
        ? SystemPlaybackGenericTracklistQueue.fromJSON(object.tracklist)
        : undefined,
      userInfo: isSet(object.userInfo)
        ? Buffer.from(bytesFromBase64(object.userInfo))
        : Buffer.alloc(0),
      metrics: isSet(object.metrics)
        ? Buffer.from(bytesFromBase64(object.metrics))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: SystemPlaybackQueue): unknown {
    const obj: any = {};
    if (message.type !== undefined && message.type !== 1) {
      obj.type = playbackQueueType_EnumToJSON(message.type);
    }
    if (message.replaceIntent !== undefined && message.replaceIntent !== 1) {
      obj.replaceIntent = replaceIntent_EnumToJSON(message.replaceIntent);
    }
    if (
      message.isRequestingImmediatePlayback !== undefined &&
      message.isRequestingImmediatePlayback !== false
    ) {
      obj.isRequestingImmediatePlayback = message.isRequestingImmediatePlayback;
    }
    if (message.featureName !== undefined && message.featureName !== "") {
      obj.featureName = message.featureName;
    }
    if (message.customData !== undefined) {
      obj.customData = SystemPlaybackCustomDataQueue.toJSON(message.customData);
    }
    if (message.tracklist !== undefined) {
      obj.tracklist = SystemPlaybackGenericTracklistQueue.toJSON(
        message.tracklist
      );
    }
    if (message.userInfo !== undefined && message.userInfo.length !== 0) {
      obj.userInfo = base64FromBytes(message.userInfo);
    }
    if (message.metrics !== undefined && message.metrics.length !== 0) {
      obj.metrics = base64FromBytes(message.metrics);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SystemPlaybackQueue>, I>>(
    base?: I
  ): SystemPlaybackQueue {
    return SystemPlaybackQueue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SystemPlaybackQueue>, I>>(
    object: I
  ): SystemPlaybackQueue {
    const message = createBaseSystemPlaybackQueue();
    message.type = object.type ?? 1;
    message.replaceIntent = object.replaceIntent ?? 1;
    message.isRequestingImmediatePlayback =
      object.isRequestingImmediatePlayback ?? false;
    message.featureName = object.featureName ?? "";
    message.customData =
      object.customData !== undefined && object.customData !== null
        ? SystemPlaybackCustomDataQueue.fromPartial(object.customData)
        : undefined;
    message.tracklist =
      object.tracklist !== undefined && object.tracklist !== null
        ? SystemPlaybackGenericTracklistQueue.fromPartial(object.tracklist)
        : undefined;
    message.userInfo = object.userInfo ?? Buffer.alloc(0);
    message.metrics = object.metrics ?? Buffer.alloc(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
