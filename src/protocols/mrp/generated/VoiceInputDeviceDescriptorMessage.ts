// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: VoiceInputDeviceDescriptorMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AudioFormatSettings } from "./AudioFormatSettingsMessage";

export const protobufPackage = "";

export interface VoiceInputDeviceDescriptor {
  defaultFormat?: AudioFormatSettings | undefined;
  supportedFormats: AudioFormatSettings[];
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseVoiceInputDeviceDescriptor(): VoiceInputDeviceDescriptor {
  return { defaultFormat: undefined, supportedFormats: [], _unknownFields: {} };
}

export const VoiceInputDeviceDescriptor: MessageFns<VoiceInputDeviceDescriptor> =
  {
    encode(
      message: VoiceInputDeviceDescriptor,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      if (message.defaultFormat !== undefined) {
        AudioFormatSettings.encode(
          message.defaultFormat,
          writer.uint32(10).fork()
        ).join();
      }
      for (const v of message.supportedFormats) {
        AudioFormatSettings.encode(v!, writer.uint32(18).fork()).join();
      }
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): VoiceInputDeviceDescriptor {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVoiceInputDeviceDescriptor();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.defaultFormat = AudioFormatSettings.decode(
              reader,
              reader.uint32()
            );
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.supportedFormats.push(
              AudioFormatSettings.decode(reader, reader.uint32())
            );
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(object: any): VoiceInputDeviceDescriptor {
      return {
        defaultFormat: isSet(object.defaultFormat)
          ? AudioFormatSettings.fromJSON(object.defaultFormat)
          : undefined,
        supportedFormats: globalThis.Array.isArray(object?.supportedFormats)
          ? object.supportedFormats.map((e: any) =>
              AudioFormatSettings.fromJSON(e)
            )
          : [],
      };
    },

    toJSON(message: VoiceInputDeviceDescriptor): unknown {
      const obj: any = {};
      if (message.defaultFormat !== undefined) {
        obj.defaultFormat = AudioFormatSettings.toJSON(message.defaultFormat);
      }
      if (message.supportedFormats?.length) {
        obj.supportedFormats = message.supportedFormats.map((e) =>
          AudioFormatSettings.toJSON(e)
        );
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<VoiceInputDeviceDescriptor>, I>>(
      base?: I
    ): VoiceInputDeviceDescriptor {
      return VoiceInputDeviceDescriptor.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<VoiceInputDeviceDescriptor>, I>>(
      object: I
    ): VoiceInputDeviceDescriptor {
      const message = createBaseVoiceInputDeviceDescriptor();
      message.defaultFormat =
        object.defaultFormat !== undefined && object.defaultFormat !== null
          ? AudioFormatSettings.fromPartial(object.defaultFormat)
          : undefined;
      message.supportedFormats =
        object.supportedFormats?.map((e) =>
          AudioFormatSettings.fromPartial(e)
        ) || [];
      return message;
    },
  };

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
