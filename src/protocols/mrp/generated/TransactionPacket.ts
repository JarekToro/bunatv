// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: TransactionPacket.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { TransactionKey } from './TransactionKey'

export const protobufPackage = ''

export interface TransactionPacket {
  key?: TransactionKey | undefined
  packetData?: Buffer | undefined
  identifier?: string | undefined
  totalLength?: number | undefined
  totalWritePosition?: number | undefined
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined
}

function createBaseTransactionPacket(): TransactionPacket {
  return {
    key: undefined,
    packetData: Buffer.alloc(0),
    identifier: '',
    totalLength: 0,
    totalWritePosition: 0,
    _unknownFields: {},
  }
}

export const TransactionPacket: MessageFns<TransactionPacket> = {
  encode(message: TransactionPacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined) {
      TransactionKey.encode(message.key, writer.uint32(10).fork()).join()
    }
    if (message.packetData !== undefined && message.packetData.length !== 0) {
      writer.uint32(18).bytes(message.packetData)
    }
    if (message.identifier !== undefined && message.identifier !== '') {
      writer.uint32(26).string(message.identifier)
    }
    if (message.totalLength !== undefined && message.totalLength !== 0) {
      writer.uint32(32).uint64(message.totalLength)
    }
    if (message.totalWritePosition !== undefined && message.totalWritePosition !== 0) {
      writer.uint32(40).uint64(message.totalWritePosition)
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10)
        for (const value of values) {
          writer.uint32(tag).raw(value)
        }
      }
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransactionPacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseTransactionPacket()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.key = TransactionKey.decode(reader, reader.uint32())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.packetData = Buffer.from(reader.bytes())
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.identifier = reader.string()
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.totalLength = longToNumber(reader.uint64())
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.totalWritePosition = longToNumber(reader.uint64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      const buf = reader.skip(tag & 7)

      const list = message._unknownFields![tag]

      if (list === undefined) {
        message._unknownFields![tag] = [buf]
      } else {
        list.push(buf)
      }
    }
    return message
  },

  fromJSON(object: any): TransactionPacket {
    return {
      key: isSet(object.key) ? TransactionKey.fromJSON(object.key) : undefined,
      packetData: isSet(object.packetData)
        ? Buffer.from(bytesFromBase64(object.packetData))
        : Buffer.alloc(0),
      identifier: isSet(object.identifier) ? globalThis.String(object.identifier) : '',
      totalLength: isSet(object.totalLength) ? globalThis.Number(object.totalLength) : 0,
      totalWritePosition: isSet(object.totalWritePosition)
        ? globalThis.Number(object.totalWritePosition)
        : 0,
    }
  },

  toJSON(message: TransactionPacket): unknown {
    const obj: any = {}
    if (message.key !== undefined) {
      obj.key = TransactionKey.toJSON(message.key)
    }
    if (message.packetData !== undefined && message.packetData.length !== 0) {
      obj.packetData = base64FromBytes(message.packetData)
    }
    if (message.identifier !== undefined && message.identifier !== '') {
      obj.identifier = message.identifier
    }
    if (message.totalLength !== undefined && message.totalLength !== 0) {
      obj.totalLength = Math.round(message.totalLength)
    }
    if (message.totalWritePosition !== undefined && message.totalWritePosition !== 0) {
      obj.totalWritePosition = Math.round(message.totalWritePosition)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<TransactionPacket>, I>>(base?: I): TransactionPacket {
    return TransactionPacket.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<TransactionPacket>, I>>(object: I): TransactionPacket {
    const message = createBaseTransactionPacket()
    message.key =
      object.key !== undefined && object.key !== null
        ? TransactionKey.fromPartial(object.key)
        : undefined
    message.packetData = object.packetData ?? Buffer.alloc(0)
    message.identifier = object.identifier ?? ''
    message.totalLength = object.totalLength ?? 0
    message.totalWritePosition = object.totalWritePosition ?? 0
    return message
  },
}

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, 'base64'))
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString('base64')
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
