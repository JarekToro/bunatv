// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: SetArtworkMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface SetArtworkMessage {
  jpegData?: Buffer | undefined;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseSetArtworkMessage(): SetArtworkMessage {
  return { jpegData: Buffer.alloc(0), _unknownFields: {} };
}

export const SetArtworkMessage: MessageFns<SetArtworkMessage> = {
  encode(
    message: SetArtworkMessage,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.jpegData !== undefined && message.jpegData.length !== 0) {
      writer.uint32(10).bytes(message.jpegData);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of globalThis.Object.entries(
        message._unknownFields
      )) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag).raw(value);
        }
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetArtworkMessage {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetArtworkMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jpegData = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const buf = reader.skip(tag & 7);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): SetArtworkMessage {
    return {
      jpegData: isSet(object.jpegData)
        ? Buffer.from(bytesFromBase64(object.jpegData))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: SetArtworkMessage): unknown {
    const obj: any = {};
    if (message.jpegData !== undefined && message.jpegData.length !== 0) {
      obj.jpegData = base64FromBytes(message.jpegData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetArtworkMessage>, I>>(
    base?: I
  ): SetArtworkMessage {
    return SetArtworkMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetArtworkMessage>, I>>(
    object: I
  ): SetArtworkMessage {
    const message = createBaseSetArtworkMessage();
    message.jpegData = object.jpegData ?? Buffer.alloc(0);
    return message;
  },
};

export const setArtworkMessage: Extension<SetArtworkMessage | undefined> = {
  number: 10,
  tag: 82,
  repeated: false,
  packed: false,
  encode: (value: SetArtworkMessage | undefined): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    const writer = new BinaryWriter();
    SetArtworkMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (tag: number, input: Uint8Array[]): SetArtworkMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return SetArtworkMessage.decode(reader, reader.uint32());
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
