// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: ModifyOutputContextRequestMessage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface ModifyOutputContextRequestType {
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export enum ModifyOutputContextRequestType_Enum {
  SharedAudioPresentation = 1,
  UNRECOGNIZED = -1,
}

export function modifyOutputContextRequestType_EnumFromJSON(
  object: any
): ModifyOutputContextRequestType_Enum {
  switch (object) {
    case 1:
    case "SharedAudioPresentation":
      return ModifyOutputContextRequestType_Enum.SharedAudioPresentation;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModifyOutputContextRequestType_Enum.UNRECOGNIZED;
  }
}

export function modifyOutputContextRequestType_EnumToJSON(
  object: ModifyOutputContextRequestType_Enum
): string {
  switch (object) {
    case ModifyOutputContextRequestType_Enum.SharedAudioPresentation:
      return "SharedAudioPresentation";
    case ModifyOutputContextRequestType_Enum.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ModifyOutputContextRequestMessage {
  type?: ModifyOutputContextRequestType_Enum | undefined;
  addingDevices: string[];
  removingDevices: string[];
  settingDevices: string[];
  clusterAwareAddingDevices: string[];
  clusterAwareRemovingDevices: string[];
  clusterAwareSettingDevices: string[];
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseModifyOutputContextRequestType(): ModifyOutputContextRequestType {
  return { _unknownFields: {} };
}

export const ModifyOutputContextRequestType: MessageFns<ModifyOutputContextRequestType> =
  {
    encode(
      message: ModifyOutputContextRequestType,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): ModifyOutputContextRequestType {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseModifyOutputContextRequestType();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(_: any): ModifyOutputContextRequestType {
      return {};
    },

    toJSON(_: ModifyOutputContextRequestType): unknown {
      const obj: any = {};
      return obj;
    },

    create<I extends Exact<DeepPartial<ModifyOutputContextRequestType>, I>>(
      base?: I
    ): ModifyOutputContextRequestType {
      return ModifyOutputContextRequestType.fromPartial(base ?? ({} as any));
    },
    fromPartial<
      I extends Exact<DeepPartial<ModifyOutputContextRequestType>, I>,
    >(_: I): ModifyOutputContextRequestType {
      const message = createBaseModifyOutputContextRequestType();
      return message;
    },
  };

function createBaseModifyOutputContextRequestMessage(): ModifyOutputContextRequestMessage {
  return {
    type: 1,
    addingDevices: [],
    removingDevices: [],
    settingDevices: [],
    clusterAwareAddingDevices: [],
    clusterAwareRemovingDevices: [],
    clusterAwareSettingDevices: [],
    _unknownFields: {},
  };
}

export const ModifyOutputContextRequestMessage: MessageFns<ModifyOutputContextRequestMessage> =
  {
    encode(
      message: ModifyOutputContextRequestMessage,
      writer: BinaryWriter = new BinaryWriter()
    ): BinaryWriter {
      if (message.type !== undefined && message.type !== 1) {
        writer.uint32(8).int32(message.type);
      }
      for (const v of message.addingDevices) {
        writer.uint32(18).string(v!);
      }
      for (const v of message.removingDevices) {
        writer.uint32(26).string(v!);
      }
      for (const v of message.settingDevices) {
        writer.uint32(34).string(v!);
      }
      for (const v of message.clusterAwareAddingDevices) {
        writer.uint32(42).string(v!);
      }
      for (const v of message.clusterAwareRemovingDevices) {
        writer.uint32(50).string(v!);
      }
      for (const v of message.clusterAwareSettingDevices) {
        writer.uint32(58).string(v!);
      }
      if (message._unknownFields !== undefined) {
        for (const [key, values] of globalThis.Object.entries(
          message._unknownFields
        )) {
          const tag = parseInt(key, 10);
          for (const value of values) {
            writer.uint32(tag).raw(value);
          }
        }
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number
    ): ModifyOutputContextRequestMessage {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseModifyOutputContextRequestMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.addingDevices.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.removingDevices.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.settingDevices.push(reader.string());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.clusterAwareAddingDevices.push(reader.string());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.clusterAwareRemovingDevices.push(reader.string());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.clusterAwareSettingDevices.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        const buf = reader.skip(tag & 7);

        const list = message._unknownFields![tag];

        if (list === undefined) {
          message._unknownFields![tag] = [buf];
        } else {
          list.push(buf);
        }
      }
      return message;
    },

    fromJSON(object: any): ModifyOutputContextRequestMessage {
      return {
        type: isSet(object.type)
          ? modifyOutputContextRequestType_EnumFromJSON(object.type)
          : 1,
        addingDevices: globalThis.Array.isArray(object?.addingDevices)
          ? object.addingDevices.map((e: any) => globalThis.String(e))
          : [],
        removingDevices: globalThis.Array.isArray(object?.removingDevices)
          ? object.removingDevices.map((e: any) => globalThis.String(e))
          : [],
        settingDevices: globalThis.Array.isArray(object?.settingDevices)
          ? object.settingDevices.map((e: any) => globalThis.String(e))
          : [],
        clusterAwareAddingDevices: globalThis.Array.isArray(
          object?.clusterAwareAddingDevices
        )
          ? object.clusterAwareAddingDevices.map((e: any) =>
              globalThis.String(e)
            )
          : [],
        clusterAwareRemovingDevices: globalThis.Array.isArray(
          object?.clusterAwareRemovingDevices
        )
          ? object.clusterAwareRemovingDevices.map((e: any) =>
              globalThis.String(e)
            )
          : [],
        clusterAwareSettingDevices: globalThis.Array.isArray(
          object?.clusterAwareSettingDevices
        )
          ? object.clusterAwareSettingDevices.map((e: any) =>
              globalThis.String(e)
            )
          : [],
      };
    },

    toJSON(message: ModifyOutputContextRequestMessage): unknown {
      const obj: any = {};
      if (message.type !== undefined && message.type !== 1) {
        obj.type = modifyOutputContextRequestType_EnumToJSON(message.type);
      }
      if (message.addingDevices?.length) {
        obj.addingDevices = message.addingDevices;
      }
      if (message.removingDevices?.length) {
        obj.removingDevices = message.removingDevices;
      }
      if (message.settingDevices?.length) {
        obj.settingDevices = message.settingDevices;
      }
      if (message.clusterAwareAddingDevices?.length) {
        obj.clusterAwareAddingDevices = message.clusterAwareAddingDevices;
      }
      if (message.clusterAwareRemovingDevices?.length) {
        obj.clusterAwareRemovingDevices = message.clusterAwareRemovingDevices;
      }
      if (message.clusterAwareSettingDevices?.length) {
        obj.clusterAwareSettingDevices = message.clusterAwareSettingDevices;
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<ModifyOutputContextRequestMessage>, I>>(
      base?: I
    ): ModifyOutputContextRequestMessage {
      return ModifyOutputContextRequestMessage.fromPartial(base ?? ({} as any));
    },
    fromPartial<
      I extends Exact<DeepPartial<ModifyOutputContextRequestMessage>, I>,
    >(object: I): ModifyOutputContextRequestMessage {
      const message = createBaseModifyOutputContextRequestMessage();
      message.type = object.type ?? 1;
      message.addingDevices = object.addingDevices?.map((e) => e) || [];
      message.removingDevices = object.removingDevices?.map((e) => e) || [];
      message.settingDevices = object.settingDevices?.map((e) => e) || [];
      message.clusterAwareAddingDevices =
        object.clusterAwareAddingDevices?.map((e) => e) || [];
      message.clusterAwareRemovingDevices =
        object.clusterAwareRemovingDevices?.map((e) => e) || [];
      message.clusterAwareSettingDevices =
        object.clusterAwareSettingDevices?.map((e) => e) || [];
      return message;
    },
  };

export const modifyOutputContextRequestMessage: Extension<
  ModifyOutputContextRequestMessage | undefined
> = {
  number: 52,
  tag: 418,
  repeated: false,
  packed: false,
  encode: (
    value: ModifyOutputContextRequestMessage | undefined
  ): Uint8Array[] => {
    const encoded: Uint8Array[] = [];
    const writer = new BinaryWriter();
    ModifyOutputContextRequestMessage.encode(value, writer.fork()).join();
    encoded.push(writer.finish());
    return encoded;
  },
  decode: (
    tag: number,
    input: Uint8Array[]
  ): ModifyOutputContextRequestMessage | undefined => {
    const reader = new BinaryReader(input[input.length - 1] ?? fail());
    return ModifyOutputContextRequestMessage.decode(reader, reader.uint32());
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface Extension<T> {
  number: number;
  tag: number;
  singularTag?: number;
  packedTag?: number;
  encode?: (message: T) => Uint8Array[];
  decode?: (tag: number, input: Uint8Array[]) => T;
  repeated: boolean;
  packed: boolean;
}

function fail(message?: string): never {
  throw new globalThis.Error(message ?? "Failed");
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
